APP_NAME=Splitzy Python Service
APP_ENV=development
APP_DEBUG=true
APP_PORT=8001
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=splitzy

# PostgreSQL reporting warehouse (Power BI + ETL). Optional for API-only.
# Example with docker-compose.reporting.yml:
# REPORTING_DATABASE_URL=postgresql://splitzy:splitzy@localhost:5433/splitzy_reporting
REPORTING_DATABASE_URL=
